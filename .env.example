# PostgreSQL
POSTGRES_USER=unap
POSTGRES_PASSWORD=unap123
POSTGRES_DB=unap_repo
POSTGRES_PORT=5432

# Application database connection
DATABASE_URL=postgresql://unap:unap123@localhost:5432/unap_repo
DB_CONNECT_TIMEOUT=10
DB_APPLICATION_NAME=unap-reco-api

# Semantic model and index
MODEL_DIR=models_semantic
MODEL_NAME=BAAI/bge-m3
EMBED_DEVICE=cpu
USE_FAISS_GPU=0

# API
API_HOST=0.0.0.0
API_PORT=8000
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,https://repositorio.unap.edu.pe,https://repositorio.unsa.edu.pe,https://repositorio.unsaac.edu.pe
